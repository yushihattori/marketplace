// import {Meteor} from 'meteor/meteor';
// import Listings from '../Listings';
//
// if (Meteor.isServer) {
//   Listings.rawCollection().createIndex({"itemname":"text", "details":"text", "seller":"text"});
// }
//
// //db.listings.createIndex({"itemname":"text", "details":"text", "seller":"text"})
